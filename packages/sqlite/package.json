{
  "name": "@pomade/sqlite",
  "version": "0.0.3",
  "author": "hodlbod",
  "license": "MIT",
  "description": "SQLite storage adapter for pomade",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "type": "module",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile --force",
    "clean": "rimraf ./dist",
    "compile": "tsc -b"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "rimraf": "~6.0.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@pomade/core": "workspace:*",
    "@welshman/lib": "^0.7.1",
    "better-sqlite3": "^11.8.1"
  },
  "peerDependencies": {
    "@pomade/core": "workspace:*"
  }
}
